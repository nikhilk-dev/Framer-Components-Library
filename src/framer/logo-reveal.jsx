// @ts-nocheck
/* eslint-disable */
/* This file was generated by Unframer for Framer project fbc126bea31ebbe6 "Untitled", do not edit manually */
"use client";

/**
 * @typedef Locale
 * string
 */

/**
 * @typedef {{
 * children?: React.ReactNode
 * locale?: Locale
 * style?: React.CSSProperties
 * className?: string
 * id?: string
 * ref?: any
 * width?: any
 * height?: any
 * layoutId?: string
 * logoInput?: boolean // Logo Input
 * uploadLogo?: string // Upload Logo
 * customLogoSVG?: string // Custom Logo SVG
 * size?: 'Small' | 'Medium' | 'Large' | 'Extra Large' // Size
 * duration?: number // Duration
 * strokeColor?: string // Stroke Color
 * fillColor?: string // Fill Color
 * strokeWidth?: number // Stroke Width
 * autoPlay?: boolean // Auto Play
}} Props

 */
import { routes } from "./chunks/chunk-2F3PHDTH.js";

// virtual:logo-reveal
import { Fragment } from "react";
import { ContextProviders } from "unframer";

// /:https://framerusercontent.com/modules/KsKl2I472N7LxkSBYsSR/VzCJe5fQgdsk1GlRf1YT/LogoReveal.js
import { jsx as _jsx } from "react/jsx-runtime";
import { useEffect, useState, useRef, startTransition } from "react";
import { addPropertyControls, ControlType } from "unframer";
var defaultLogo = `<svg viewBox="0 0 100 100" xmlns="http://www.w3.org/2000/svg">
  <circle cx="50" cy="50" r="40" fill="none" stroke="currentColor" stroke-width="2"/>
  <path d="M50 30 L50 70 M30 50 L70 50" stroke="currentColor" stroke-width="2"/>
</svg>`;
function sanitizeSvg(svg) {
	return svg.trim();
}
function LogoReveal({
	// Content
	logoSvg = "",
	logoSize = "M",
	useFileUpload = false,
	logoFile = "",
	// Animation
	animationDuration = 2,
	strokeColor = "#000000",
	fillColor = "#000000",
	strokeWidth = 2,
	autoPlay = true,
}) {
	const [isAnimating, setIsAnimating] = useState(autoPlay);
	const containerRef = useRef(null);
	const logoContent = useFileUpload
		? logoFile
			? sanitizeSvg(logoFile)
			: defaultLogo
		: logoSvg
			? sanitizeSvg(logoSvg)
			: defaultLogo;
	const sizeMap = {
		S: 100,
		M: 200,
		L: 300,
		XL: 400,
	};
	const size = sizeMap[logoSize];
	useEffect(() => {
		if (autoPlay) {
			startTransition(() => setIsAnimating(true));
		}
	}, [autoPlay]);
	return (
		<div
			ref={containerRef}
			style={{
				width: size,
				height: size,
				position: "relative",
				display: "flex",
				alignItems: "center",
				justifyContent: "center",
			}}
		>
			<div
				style={{
					width: "100%",
					height: "100%",
					color: fillColor,
				}}
				dangerouslySetInnerHTML={{
					__html: logoContent,
				}}
			/>
		</div>
	);
}
addPropertyControls(LogoReveal, {
	// Content Group
	useFileUpload: {
		type: ControlType.Boolean,
		title: "Logo Input",
		defaultValue: false,
		enabledTitle: "Upload",
		disabledTitle: "Type",
	},
	logoFile: {
		type: ControlType.File,
		title: "Upload Logo",
		allowedFileTypes: ["svg"],
		hidden: (props) => !props.useFileUpload,
	},
	logoSvg: {
		type: ControlType.String,
		title: "Custom Logo SVG",
		defaultValue: "",
		displayTextArea: true,
		hidden: (props) => props.useFileUpload,
	},
	logoSize: {
		type: ControlType.Enum,
		title: "Size",
		options: ["S", "M", "L", "XL"],
		optionTitles: ["Small", "Medium", "Large", "Extra Large"],
		defaultValue: "M",
		displaySegmentedControl: true,
	},
	// Animation Group
	animationDuration: {
		type: ControlType.Number,
		title: "Duration",
		defaultValue: 2,
		min: 0.5,
		max: 10,
		step: 0.5,
		unit: "s",
	},
	strokeColor: {
		type: ControlType.Color,
		title: "Stroke Color",
		defaultValue: "#000000",
	},
	fillColor: {
		type: ControlType.Color,
		title: "Fill Color",
		defaultValue: "#000000",
	},
	strokeWidth: {
		type: ControlType.Number,
		title: "Stroke Width",
		defaultValue: 2,
		min: 1,
		max: 10,
		step: 1,
	},
	autoPlay: {
		type: ControlType.Boolean,
		title: "Auto Play",
		defaultValue: true,
		enabledTitle: "On",
		disabledTitle: "Off",
	},
});

// virtual:logo-reveal
import { WithFramerBreakpoints } from "unframer";
import { jsx } from "react/jsx-runtime";
var locales = [];
var defaultResponsiveVariants = {};
/** @type {function(Props): any} */
function ComponentWithRoot({ locale, ...rest }) {
	return (
		<ContextProviders
			routes={routes}
			framerSiteId={
				"fbc126bea31ebbe6cf64cc6db71380905aa865df749edb6fe0a120a118d79a8c"
			}
			locale={locale}
			locales={locales}
		>
			<LogoReveal {...rest} />
		</ContextProviders>
	);
}
/**
 * @type {import("unframer").UnframerBreakpoint}
 * Represents a responsive breakpoint for unframer.
 */

/**
 * @typedef VariantsMap
 * Partial record of UnframerBreakpoint to Props.variant, with a mandatory 'base' key.
 * { [key in UnframerBreakpoint]?: Props['variant'] } & { base: Props['variant'] }
 */

/**
 * Renders LogoRevealFramerComponent for all breakpoints with a variants map. Variant prop is inferred per breakpoint.
 * @function
 * @param {Omit<Props, 'variant'> & {variants?: VariantsMap}} props
 * @returns {any}
 */
ComponentWithRoot.Responsive = ({ locale = "", ...rest }) => {
	return (
		<ContextProviders
			routes={routes}
			framerSiteId={
				"fbc126bea31ebbe6cf64cc6db71380905aa865df749edb6fe0a120a118d79a8c"
			}
			locale={locale}
			locales={locales}
		>
			<WithFramerBreakpoints
				Component={LogoReveal}
				variants={defaultResponsiveVariants}
				{...rest}
			/>
		</ContextProviders>
	);
};
Object.assign(ComponentWithRoot, LogoReveal);
var logo_reveal_default = ComponentWithRoot;
export { logo_reveal_default as default };
