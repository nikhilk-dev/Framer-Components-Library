// @ts-nocheck
/* eslint-disable */
/* This file was generated by Unframer for Framer project fbc126bea31ebbe6 "Untitled", do not edit manually */
"use client";

/**
 * @typedef Locale
 * string
 */

/**
 * @typedef {{
 * children?: React.ReactNode
 * locale?: Locale
 * style?: React.CSSProperties
 * className?: string
 * id?: string
 * ref?: any
 * width?: any
 * height?: any
 * layoutId?: string
 * items?: {label: string, id: string[] // Items
 * horizontal?: 'Left' | 'Center' | 'Right' // Horizontal
 * vertical?: 'Top' | 'Center' | 'Bottom' // Vertical
 * activeIndex?: number // Active Index
 * lineColor?: string // Line Color
 * activeColor?: string // Active Color
 * lineWidth?: number // Line Width
 * lineHeight?: number // Line Height
 * activeWidth?: number // Active Width
 * activeHeight?: number // Active Height
 * spacing?: number // Spacing
 * background?: string // Background
}} Props

 */
import { routes } from "./chunks/chunk-2F3PHDTH.js";

// virtual:sticky-navigation-1
import { Fragment } from "react";
import { ContextProviders } from "unframer";

// /:https://framerusercontent.com/modules/HNjc3lhWLiib4vdz5Byl/8MjNQT5H6jEIagoMefyN/StickyNavigation_1.js
import { jsx as _jsx } from "react/jsx-runtime";
import { addPropertyControls, ControlType } from "unframer";
import { useState, startTransition, useEffect } from "react";
var __unframerWindow = typeof window !== "undefined" ? window : void 0;
function StickyNavigation(props) {
	const {
		items,
		activeIndex = 2,
		lineColor,
		activeLineColor,
		lineWidth,
		lineHeight,
		activeLineWidth,
		activeLineHeight,
		spacing,
		backgroundColor,
		horizontalAlignment,
		verticalAlignment,
	} = props;
	const [hoveredIndex, setHoveredIndex] = useState(null);
	const [selectedIndex, setSelectedIndex] = useState(activeIndex);
	useEffect(() => {
		if (typeof __unframerWindow === "undefined") return;
		const observerOptions = {
			root: null,
			rootMargin: "-50% 0px -50% 0px",
			threshold: 0,
		};
		const observerCallback = (entries) => {
			entries.forEach((entry) => {
				if (entry.isIntersecting) {
					const index = items.findIndex((item) => item.id === entry.target.id);
					if (index !== -1) {
						startTransition(() => setSelectedIndex(index));
					}
				}
			});
		};
		const observer = new IntersectionObserver(
			observerCallback,
			observerOptions,
		);
		items.forEach((item) => {
			const element = document.getElementById(item.id);
			if (element) {
				observer.observe(element);
			}
		});
		return () => {
			observer.disconnect();
		};
	}, [items]);
	const handleClick = (item, index) => {
		if (typeof __unframerWindow === "undefined") return;
		startTransition(() => setSelectedIndex(index));
		const element = document.getElementById(item.id);
		if (element) {
			element.scrollIntoView({
				behavior: "smooth",
				block: "start",
			});
		}
	};
	const handleKeyDown = (e, item, index) => {
		if (e.key === "Enter" || e.key === " ") {
			e.preventDefault();
			handleClick(item, index);
		}
	};
	return (
		<div
			style={{
				width: "100%",
				height: "100%",
				backgroundColor,
				display: "flex",
				alignItems:
					verticalAlignment === "top"
						? "flex-start"
						: verticalAlignment === "bottom"
							? "flex-end"
							: "center",
				justifyContent: "center",
				position: "relative",
			}}
		>
			<div
				style={{
					display: "flex",
					flexDirection: "column",
					gap: spacing,
					alignItems:
						horizontalAlignment === "left"
							? "flex-start"
							: horizontalAlignment === "right"
								? "flex-end"
								: "center",
				}}
			>
				{items.map((item, index) => {
					const isActive = index === selectedIndex;
					const isHovered = index === hoveredIndex;
					return (
						<div
							style={{
								width: isActive ? activeLineWidth : lineWidth,
								height: isActive ? activeLineHeight : lineHeight,
								backgroundColor: isActive ? activeLineColor : lineColor,
								cursor: "pointer",
								transition: "all 0.2s ease",
								transform: isHovered && !isActive ? "scaleX(1.2)" : "scaleX(1)",
							}}
							onMouseEnter={() => startTransition(() => setHoveredIndex(index))}
							onMouseLeave={() => startTransition(() => setHoveredIndex(null))}
							onClick={() => handleClick(item, index)}
							onKeyDown={(e) => handleKeyDown(e, item, index)}
							role={"button"}
							tabIndex={0}
							aria-label={item.label}
						/>
					);
				})}
			</div>
		</div>
	);
}
addPropertyControls(StickyNavigation, {
	items: {
		type: ControlType.Array,
		title: "Items",
		control: {
			type: ControlType.Object,
			controls: {
				label: {
					type: ControlType.String,
					title: "Label",
					defaultValue: "Section",
				},
				id: {
					type: ControlType.String,
					title: "ID",
					defaultValue: "section",
				},
			},
		},
		defaultValue: [
			{
				label: "Section 1",
				id: "section-1",
			},
			{
				label: "Section 2",
				id: "section-2",
			},
			{
				label: "Section 3",
				id: "section-3",
			},
			{
				label: "Section 4",
				id: "section-4",
			},
			{
				label: "Section 5",
				id: "section-5",
			},
			{
				label: "Section 6",
				id: "section-6",
			},
		],
	},
	horizontalAlignment: {
		type: ControlType.Enum,
		title: "Horizontal",
		options: ["left", "center", "right"],
		optionTitles: ["Left", "Center", "Right"],
		defaultValue: "center",
		displaySegmentedControl: true,
	},
	verticalAlignment: {
		type: ControlType.Enum,
		title: "Vertical",
		options: ["top", "center", "bottom"],
		optionTitles: ["Top", "Center", "Bottom"],
		defaultValue: "center",
		displaySegmentedControl: true,
	},
	activeIndex: {
		type: ControlType.Number,
		title: "Active Index",
		defaultValue: 2,
		min: 0,
		max: 10,
		step: 1,
		displayStepper: true,
	},
	lineColor: {
		type: ControlType.Color,
		title: "Line Color",
		defaultValue: "#FFFFFF",
	},
	activeLineColor: {
		type: ControlType.Color,
		title: "Active Color",
		defaultValue: "#FF5733",
	},
	lineWidth: {
		type: ControlType.Number,
		title: "Line Width",
		defaultValue: 30,
		min: 10,
		max: 100,
		step: 1,
		unit: "px",
	},
	lineHeight: {
		type: ControlType.Number,
		title: "Line Height",
		defaultValue: 2,
		min: 1,
		max: 10,
		step: 1,
		unit: "px",
	},
	activeLineWidth: {
		type: ControlType.Number,
		title: "Active Width",
		defaultValue: 50,
		min: 10,
		max: 100,
		step: 1,
		unit: "px",
	},
	activeLineHeight: {
		type: ControlType.Number,
		title: "Active Height",
		defaultValue: 4,
		min: 1,
		max: 10,
		step: 1,
		unit: "px",
	},
	spacing: {
		type: ControlType.Number,
		title: "Spacing",
		defaultValue: 12,
		min: 4,
		max: 40,
		step: 1,
		unit: "px",
	},
	backgroundColor: {
		type: ControlType.Color,
		title: "Background",
		defaultValue: "#000000",
	},
});

// virtual:sticky-navigation-1
import { WithFramerBreakpoints } from "unframer";
import { jsx } from "react/jsx-runtime";
var locales = [];
var defaultResponsiveVariants = {};
/** @type {function(Props): any} */
function ComponentWithRoot({ locale, ...rest }) {
	return (
		<ContextProviders
			routes={routes}
			framerSiteId={
				"fbc126bea31ebbe6cf64cc6db71380905aa865df749edb6fe0a120a118d79a8c"
			}
			locale={locale}
			locales={locales}
		>
			<StickyNavigation {...rest} />
		</ContextProviders>
	);
}
/**
 * @type {import("unframer").UnframerBreakpoint}
 * Represents a responsive breakpoint for unframer.
 */

/**
 * @typedef VariantsMap
 * Partial record of UnframerBreakpoint to Props.variant, with a mandatory 'base' key.
 * { [key in UnframerBreakpoint]?: Props['variant'] } & { base: Props['variant'] }
 */

/**
 * Renders StickyNavigation1FramerComponent for all breakpoints with a variants map. Variant prop is inferred per breakpoint.
 * @function
 * @param {Omit<Props, 'variant'> & {variants?: VariantsMap}} props
 * @returns {any}
 */
ComponentWithRoot.Responsive = ({ locale = "", ...rest }) => {
	return (
		<ContextProviders
			routes={routes}
			framerSiteId={
				"fbc126bea31ebbe6cf64cc6db71380905aa865df749edb6fe0a120a118d79a8c"
			}
			locale={locale}
			locales={locales}
		>
			<WithFramerBreakpoints
				Component={StickyNavigation}
				variants={defaultResponsiveVariants}
				{...rest}
			/>
		</ContextProviders>
	);
};
Object.assign(ComponentWithRoot, StickyNavigation);
var sticky_navigation_1_default = ComponentWithRoot;
export { sticky_navigation_1_default as default };
