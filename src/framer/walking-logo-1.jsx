// @ts-nocheck
/* eslint-disable */
/* This file was generated by Unframer for Framer project fbc126bea31ebbe6 "Untitled", do not edit manually */
"use client";

/**
 * @typedef Locale
 * string
 */

/**
 * @typedef {{
 * children?: React.ReactNode
 * locale?: Locale
 * style?: React.CSSProperties
 * className?: string
 * id?: string
 * ref?: any
 * width?: any
 * height?: any
 * layoutId?: string
 * showBubbleLive?: boolean // Show Bubble Live
 * flipLogo?: boolean // Flip Logo
 * sleeping?: boolean // Sleeping
 * enableMovement?: boolean // Enable Movement
 * showBee?: boolean // Show Bee
 * beeColor?: string // Bee Color
 * beeSize?: number // Bee Size
 * hoverMessages?: {text: string[] // Hover Messages
 * messageFontSize?: number // Message Font Size
 * enableBlink?: boolean // Enable Blink
 * blinkInterval?: number // Blink Interval
 * logoSize?: number // Logo Size
 * logoColor?: string // Logo Color
 * buttonFont?: undefined // Button Font
 * buttonColor?: string // Button Color
 * buttonTextColor?: string // Button Text Color
 * buttonPadding?: number // Button Padding
 * buttonOffsetX?: number // Button Offset X
 * buttonOffsetY?: number // Button Offset Y
}} Props

 */
import { routes } from "./chunks/chunk-2F3PHDTH.js";

// virtual:walking-logo-1
import { Fragment } from "react";
import { ContextProviders } from "unframer";

// /:https://framerusercontent.com/modules/LDKvvwpTVCtVizB0Pnb0/lIyihygdIwOaMysBfs3n/WalkingLogo_1.js
import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import {
	addPropertyControls,
	ControlType,
	useIsStaticRenderer,
} from "unframer";
import { useState, useEffect, startTransition } from "react";
function WalkingLogo(props) {
	const {
		logoColor = "#000000",
		logoScale = 1,
		buttonOffsetX = 20,
		buttonOffsetY = 0,
		buttonPadding = 10,
		showBubbleLive = false,
		buttonFont,
		enableBlink = true,
		blinkInterval = 3,
		hoverMessages = [
			{
				text: "what?",
			},
			{
				text: "hello!",
			},
			{
				text: "need help?",
			},
		],
		hoverMessageFontSize = 14,
		enableMovement = true,
		flipLogo = false,
		showBee = false,
		beeColor = "#FFD700",
		beeSize = 1,
		isSleeping = false,
		style,
	} = props;
	const isStatic = useIsStaticRenderer();
	const [showButton, setShowButton] = useState(false);
	const [hoverCount, setHoverCount] = useState(0);
	const [animationDelay, setAnimationDelay] = useState(0);
	const [isDoubleBlink, setIsDoubleBlink] = useState(false);
	const [swayDelay, setSwayDelay] = useState(0);
	const [lookDirection, setLookDirection] = useState(0);
	const [beePosition, setBeePosition] = useState({
		x: 100,
		y: -50,
	});
	const [beeAngle, setBeeAngle] = useState(0);
	const shouldAnimate = enableBlink && !isStatic;
	const shouldMove = enableMovement && !isStatic;
	const currentMessageIndex = hoverCount % hoverMessages.length;
	const currentMessage = hoverMessages[currentMessageIndex]?.text || "what?";
	const logoWidth = 79 * logoScale;
	const logoHeight = 34 * logoScale;
	const logoOriginX = 39.5 * logoScale;
	const logoOriginY = 17 * logoScale;
	const handleMouseEnter = () => {
		startTransition(() => {
			setShowButton(true);
			setHoverCount((prev) => prev + 1);
		});
	};
	const handleMouseLeave = () => {
		startTransition(() => {
			setShowButton(false);
		});
	};
	const keyframesBlink = `
    @keyframes blink {
      0%, 100% { transform: scaleY(1); opacity: 1; }
      48% { transform: scaleY(1); opacity: 1; }
      50% { transform: scaleY(0.1); opacity: 0.5; }
      52% { transform: scaleY(1); opacity: 1; }
    }
  `;
	const keyframesDoubleBlink = `
    @keyframes doubleBlink {
      0%, 100% { transform: scaleY(1); opacity: 1; }
      23% { transform: scaleY(1); opacity: 1; }
      25% { transform: scaleY(0.1); opacity: 0.5; }
      27% { transform: scaleY(1); opacity: 1; }
      48% { transform: scaleY(1); opacity: 1; }
      50% { transform: scaleY(0.1); opacity: 0.5; }
      52% { transform: scaleY(1); opacity: 1; }
    }
  `;
	const keyframesSleepBreathe = `
    @keyframes sleepBreathe {
      0%, 100% { transform: scale(1) translateY(0); }
      50% { transform: scale(1.015) translateY(-1px); }
    }
  `;
	const keyframesSleepBob = `
    @keyframes sleepBob {
      0%, 100% { transform: rotate(0deg) translateY(0); }
      50% { transform: rotate(-2deg) translateY(1px); }
    }
  `;
	const keyframesSleepTwitch = `
    @keyframes sleepTwitch {
      0%, 95%, 100% { transform: rotate(0deg); }
      96%, 98% { transform: rotate(1deg); }
      97%, 99% { transform: rotate(-1deg); }
    }
  `;
	const keyframesClosedEyes = `
    @keyframes closedEyes {
      0%, 100% { transform: scaleY(0.1); opacity: 0.5; }
    }
  `;
	const keyframesBreathe = `
    @keyframes breathe {
      0%, 100% { transform: scale(1) translateY(0); }
      50% { transform: scale(1.01) translateY(-0.5px); }
    }
  `;
	const keyframesSway = `
    @keyframes sway {
      0%, 100% { transform: rotate(0deg); }
      25% { transform: rotate(0.5deg); }
      75% { transform: rotate(-0.5deg); }
    }
  `;
	const keyframesFaceTurn = `
    @keyframes faceTurn {
      0%, 100% { transform: perspective(1000px) rotateY(0deg); }
      25% { transform: perspective(1000px) rotateY(3deg); }
      75% { transform: perspective(1000px) rotateY(-3deg); }
    }
  `;
	const keyframesHeadTurn = `
    @keyframes headTurn {
      0%, 100% { transform: rotate(0deg); }
      30% { transform: rotate(1deg); }
      70% { transform: rotate(-1deg); }
    }
  `;
	const keyframesLookLeft = `
    @keyframes lookLeft {
      0% { transform: perspective(1000px) rotateY(0deg); }
      100% { transform: perspective(1000px) rotateY(-15deg); }
    }
  `;
	const keyframesLookRight = `
    @keyframes lookRight {
      0% { transform: perspective(1000px) rotateY(0deg); }
      100% { transform: perspective(1000px) rotateY(15deg); }
    }
  `;
	const keyframesLookCenter = `
    @keyframes lookCenter {
      0% { transform: perspective(1000px) rotateY(0deg); }
      100% { transform: perspective(1000px) rotateY(0deg); }
    }
  `;
	const keyframesBeeWings = `
    @keyframes beeWings {
      0%, 100% { transform: scaleY(1); }
      50% { transform: scaleY(0.3); }
    }
  `;
	useEffect(() => {
		if (!shouldAnimate) return;
		const randomizeDelay = () => {
			const randomDelay = Math.random() * blinkInterval;
			const shouldDoubleBlink = Math.random() < 0.3;
			startTransition(() => {
				setAnimationDelay(randomDelay);
				setIsDoubleBlink(shouldDoubleBlink);
			});
		};
		randomizeDelay();
		const interval = setInterval(randomizeDelay, blinkInterval * 1e3);
		return () => clearInterval(interval);
	}, [shouldAnimate, blinkInterval]);
	useEffect(() => {
		if (!shouldMove) return;
		const randomizeSway = () => {
			const randomDelay = Math.random() * 2;
			startTransition(() => {
				setSwayDelay(randomDelay);
			});
		};
		randomizeSway();
		const interval = setInterval(randomizeSway, 8e3);
		return () => clearInterval(interval);
	}, [shouldMove]);
	useEffect(() => {
		if (!shouldMove) return;
		if (isSleeping) return;
		if (showBee) {
			const logoCenter = {
				x: logoWidth / 2,
				y: logoHeight / 2,
			};
			const dx = beePosition.x - logoCenter.x;
			const dy = beePosition.y - logoCenter.y;
			const angle = Math.atan2(dy, dx) * (180 / Math.PI);
			let rotation = 0;
			if (Math.abs(dx) > 30) {
				rotation = Math.max(-15, Math.min(15, dx / 10));
			}
			startTransition(() => {
				setLookDirection(rotation);
			});
		} else {
			const randomLook = () => {
				const directions = [0, -1, 1, 0, 0];
				const randomDirection =
					directions[Math.floor(Math.random() * directions.length)];
				startTransition(() => {
					setLookDirection(randomDirection);
				});
			};
			randomLook();
			const scheduleNext = () => {
				const randomInterval = 2e3 + Math.random() * 3e3;
				setTimeout(() => {
					randomLook();
					scheduleNext();
				}, randomInterval);
			};
			scheduleNext();
		}
	}, [shouldMove, showBee, beePosition, logoWidth, logoHeight]);
	useEffect(() => {
		if (!showBee || isStatic) return;
		let animationFrame;
		let startTime = Date.now();
		const animateBee = () => {
			const elapsed = (Date.now() - startTime) / 1e3;
			const centerX = logoWidth / 2;
			const centerY = logoHeight / 2;
			const radiusX = 80;
			const radiusY = 60;
			const speed = 0.5;
			const t = elapsed * speed;
			const x = centerX + radiusX * Math.sin(t * 2);
			const y = centerY + radiusY * Math.sin(t) * Math.cos(t);
			const prevX = centerX + radiusX * Math.sin((t - 0.05) * 2);
			const prevY = centerY + radiusY * Math.sin(t - 0.05) * Math.cos(t - 0.05);
			const angle = Math.atan2(y - prevY, x - prevX) * (180 / Math.PI);
			startTransition(() => {
				setBeePosition({
					x,
					y,
				});
				setBeeAngle(angle);
			});
			animationFrame = requestAnimationFrame(animateBee);
		};
		animateBee();
		return () => {
			if (animationFrame) cancelAnimationFrame(animationFrame);
		};
	}, [showBee, isStatic, logoWidth, logoHeight]);
	return (
		<div
			style={{
				...style,
				position: "relative",
				display: "flex",
				alignItems: "flex-end",
				overflow: "visible",
				width: logoWidth,
				height: logoHeight,
				transform: flipLogo ? "scaleX(-1)" : "scaleX(1)",
			}}
			onMouseEnter={handleMouseEnter}
			onMouseLeave={handleMouseLeave}
		>
			<style>
				{keyframesBlink}
				{keyframesDoubleBlink}
				{keyframesSleepBreathe}
				{keyframesSleepBob}
				{keyframesSleepTwitch}
				{keyframesClosedEyes}
				{keyframesBreathe}
				{keyframesSway}
				{keyframesFaceTurn}
				{keyframesHeadTurn}
				{keyframesLookLeft}
				{keyframesLookRight}
				{keyframesLookCenter}
				{keyframesBeeWings}
			</style>
			<div
				style={{
					position: "relative",
					animation: isSleeping
						? "sleepBreathe 6s ease-in-out infinite, sleepBob 8s ease-in-out infinite, sleepTwitch 15s ease-in-out infinite"
						: shouldMove
							? "breathe 4s ease-in-out infinite, sway 8s ease-in-out infinite, headTurn 7s ease-in-out infinite"
							: "none",
					animationDelay: shouldMove
						? `0s, ${swayDelay}s, ${swayDelay * 0.7}s`
						: void 0,
					transformOrigin: `${logoOriginX}px ${logoOriginY}px`,
				}}
			>
				<div
					style={{
						animation:
							!isSleeping && !showBee && shouldMove
								? lookDirection === -1
									? "lookLeft 0.5s ease-out forwards"
									: lookDirection === 1
										? "lookRight 0.5s ease-out forwards"
										: "lookCenter 0.5s ease-out forwards"
								: "none",
						transform:
							!isSleeping && showBee && shouldMove
								? `perspective(1000px) rotateY(${lookDirection}deg)`
								: void 0,
						transition: showBee ? "transform 0.3s ease-out" : void 0,
						transformOrigin: `${logoOriginX}px ${logoOriginY}px`,
					}}
				>
					<div
						style={{
							animation: isSleeping
								? "closedEyes 1s linear infinite"
								: shouldAnimate
									? `${isDoubleBlink ? "doubleBlink" : "blink"} ${blinkInterval}s ease-in-out infinite`
									: "none",
							animationDelay: shouldAnimate ? `${animationDelay}s` : void 0,
							transformOrigin: `${logoOriginX}px ${logoOriginY}px`,
						}}
					>
						<svg
							style={{
								display: "block",
								width: logoWidth,
								height: logoHeight,
							}}
							width={"79"}
							height={"34"}
							viewBox={"0 0 79 34"}
							fill={"none"}
							xmlns={"http://www.w3.org/2000/svg"}
						>
							<path
								d={
									"M61.6396 0C67.0009 2.99545e-05 71.215 1.57875 74.2471 4.7627C77.2912 7.93826 78.8076 11.9728 78.8076 16.8457C78.8075 20.0243 78.1517 22.8472 76.832 25.3066L76.8311 25.3057C75.5314 27.7651 73.6333 29.7625 71.1426 31.2959C68.6373 32.8377 65.4642 33.5996 61.6396 33.5996C59.7567 33.5996 57.6488 33.293 55.3184 32.6846L55.1006 32.6279L55.9121 27.2061L55.9561 26.915L56.2363 27.0059C58.0878 27.6023 59.9883 27.9004 61.9385 27.9004C64.1604 27.9004 66.0098 27.444 67.499 26.5459C68.9921 25.6255 70.1371 24.3332 70.9355 22.6621L70.9365 22.6602C71.7556 20.987 72.1689 19.0508 72.1689 16.8457C72.1689 13.5272 71.2747 10.8477 69.5049 8.78613L69.5039 8.78516C67.7648 6.73718 65.2561 5.69922 61.9385 5.69922C59.9681 5.69925 58.0682 5.99748 56.2373 6.59375L55.9561 6.68555L55.1006 0.97168L55.3184 0.915039C57.6684 0.306833 59.7766 0 61.6396 0Z"
								}
								fill={logoColor}
							/>
							<path
								d={
									"M12.0176 0.181641C15.9517 0.181645 19.1382 0.768145 21.5605 1.95996C23.9933 3.12674 25.7256 4.70282 26.7334 6.69629C27.7368 8.68071 28.2275 11.4261 28.2275 14.9102V33.1162H22.0078V15.4854C22.0078 13.1414 21.7494 11.3218 21.2461 10.0127C20.7534 8.73123 19.7658 7.69433 18.251 6.91016L18.248 6.90918C16.7434 6.11098 14.5552 5.69922 11.6582 5.69922C10.0333 5.69922 8.22026 5.82997 6.21875 6.0918V33.1162H0V1.27734L0.208984 1.24316C4.49169 0.536165 8.42825 0.181641 12.0176 0.181641Z"
								}
								fill={logoColor}
							/>
							<path
								d={
									"M43.4725 0.0996094L43.6394 0.569478L49.1846 16.0996H53.3076L50.2318 0.0996094H43.4725Z"
								}
								fill={logoColor}
							/>
							<path
								d={
									"M32.3076 0.0996094L38.0196 16.0996H42.1437L42.0642 15.6902L39.1149 0.349348L39.0678 0.0996094H32.3076Z"
								}
								fill={logoColor}
							/>
						</svg>
					</div>
				</div>
			</div>
			{(showButton || showBubbleLive) && (
				<div
					style={{
						position: "absolute",
						top: `calc(50% + ${buttonOffsetY}px)`,
						left: `calc(100% + ${buttonOffsetX}px)`,
						transform: "translateY(-50%)",
					}}
				>
					<button
						style={{
							position: "relative",
							padding: `${buttonPadding}px ${buttonPadding * 1.8}px`,
							backgroundColor: props.buttonColor || "#000000",
							color: props.buttonTextColor || "#FFFFFF",
							border: "none",
							borderRadius: 18,
							cursor: "pointer",
							transition: "transform 0.2s ease",
							whiteSpace: "nowrap",
							...buttonFont,
							fontSize: hoverMessageFontSize,
						}}
						onMouseEnter={(e) => {
							e.currentTarget.style.transform = "scale(1.05)";
						}}
						onMouseLeave={(e) => {
							e.currentTarget.style.transform = "scale(1)";
						}}
					>
						{currentMessage}
					</button>
					<div
						style={{
							position: "absolute",
							left: -8,
							top: "50%",
							transform: "translateY(-50%)",
							width: 0,
							height: 0,
							borderTop: "8px solid transparent",
							borderBottom: "8px solid transparent",
							borderRight: `8px solid ${props.buttonColor || "#000000"}`,
						}}
					/>
				</div>
			)}
			{showBee && (
				<div
					style={{
						position: "absolute",
						left: beePosition.x,
						top: beePosition.y,
						transform: `translate(-50%, -50%) rotate(${beeAngle}deg) scale(${beeSize})`,
						pointerEvents: "none",
						transition: "transform 0.1s linear",
					}}
				>
					<svg width={"24"} height={"24"} viewBox={"0 0 24 24"} fill={"none"}>
						<g
							style={{
								animation: "beeWings 0.1s ease-in-out infinite",
								transformOrigin: "12px 10px",
							}}
						>
							<ellipse
								cx={"8"}
								cy={"8"}
								rx={"5"}
								ry={"7"}
								fill={"rgba(255, 255, 255, 0.6)"}
								opacity={"0.8"}
							/>
							<ellipse
								cx={"16"}
								cy={"8"}
								rx={"5"}
								ry={"7"}
								fill={"rgba(255, 255, 255, 0.6)"}
								opacity={"0.8"}
							/>
						</g>
						<ellipse cx={"12"} cy={"12"} rx={"4"} ry={"6"} fill={beeColor} />
						<rect x={"10"} y={"10"} width={"4"} height={"1.5"} fill={"#000"} />
						<rect x={"10"} y={"13"} width={"4"} height={"1.5"} fill={"#000"} />
						<circle cx={"12"} cy={"7"} r={"2.5"} fill={"#000"} />
						<line
							x1={"11"}
							y1={"6"}
							x2={"10"}
							y2={"4"}
							stroke={"#000"}
							strokeWidth={"0.5"}
						/>
						<line
							x1={"13"}
							y1={"6"}
							x2={"14"}
							y2={"4"}
							stroke={"#000"}
							strokeWidth={"0.5"}
						/>
					</svg>
				</div>
			)}
			{isSleeping && (
				<div
					style={{
						position: "absolute",
						left: logoWidth + 10,
						top: -10,
						pointerEvents: "none",
					}}
				>
					<div
						style={{
							animation: "floatUp 3s ease-in-out infinite",
							opacity: 0,
						}}
					>
						<span
							style={{
								fontSize: 20 * logoScale,
								opacity: 0.6,
							}}
						>
							{"Z"}
						</span>
					</div>
					<div
						style={{
							animation: "floatUp 3s ease-in-out infinite",
							animationDelay: "1s",
							opacity: 0,
							marginLeft: 8,
						}}
					>
						<span
							style={{
								fontSize: 24 * logoScale,
								opacity: 0.5,
							}}
						>
							{"Z"}
						</span>
					</div>
					<div
						style={{
							animation: "floatUp 3s ease-in-out infinite",
							animationDelay: "2s",
							opacity: 0,
							marginLeft: 16,
						}}
					>
						<span
							style={{
								fontSize: 28 * logoScale,
								opacity: 0.4,
							}}
						>
							{"Z"}
						</span>
					</div>
				</div>
			)}
			<style>{`
					@keyframes floatUp {
						0% { transform: translateY(0); opacity: 0; }
						20% { opacity: 0.6; }
						80% { opacity: 0.3; }
						100% { transform: translateY(-40px); opacity: 0; }
					}
				`}</style>
		</div>
	);
}
addPropertyControls(WalkingLogo, {
	showBubbleLive: {
		type: ControlType.Boolean,
		title: "Show Bubble Live",
		defaultValue: false,
		enabledTitle: "Show",
		disabledTitle: "Hide",
	},
	flipLogo: {
		type: ControlType.Boolean,
		title: "Flip Logo",
		defaultValue: false,
		enabledTitle: "Flipped",
		disabledTitle: "Normal",
	},
	isSleeping: {
		type: ControlType.Boolean,
		title: "Sleeping",
		defaultValue: false,
		enabledTitle: "Asleep",
		disabledTitle: "Awake",
	},
	enableMovement: {
		type: ControlType.Boolean,
		title: "Enable Movement",
		defaultValue: true,
		enabledTitle: "On",
		disabledTitle: "Off",
	},
	showBee: {
		type: ControlType.Boolean,
		title: "Show Bee",
		defaultValue: false,
		enabledTitle: "Show",
		disabledTitle: "Hide",
	},
	beeColor: {
		type: ControlType.Color,
		title: "Bee Color",
		defaultValue: "#FFD700",
		hidden: ({ showBee }) => !showBee,
	},
	beeSize: {
		type: ControlType.Number,
		title: "Bee Size",
		defaultValue: 1,
		min: 0.5,
		max: 3,
		step: 0.1,
		hidden: ({ showBee }) => !showBee,
	},
	hoverMessages: {
		type: ControlType.Array,
		title: "Hover Messages",
		control: {
			type: ControlType.Object,
			controls: {
				text: {
					type: ControlType.String,
					title: "Message",
					defaultValue: "what?",
				},
			},
		},
		defaultValue: [
			{
				text: "what?",
			},
			{
				text: "hello!",
			},
			{
				text: "need help?",
			},
		],
	},
	hoverMessageFontSize: {
		type: ControlType.Number,
		title: "Message Font Size",
		defaultValue: 14,
		min: 8,
		max: 48,
		step: 1,
		unit: "px",
	},
	enableBlink: {
		type: ControlType.Boolean,
		title: "Enable Blink",
		defaultValue: true,
		enabledTitle: "On",
		disabledTitle: "Off",
	},
	blinkInterval: {
		type: ControlType.Number,
		title: "Blink Interval",
		defaultValue: 3,
		min: 1,
		max: 10,
		step: 0.5,
		unit: "s",
		hidden: ({ enableBlink }) => !enableBlink,
	},
	logoScale: {
		type: ControlType.Number,
		title: "Logo Size",
		defaultValue: 1,
		min: 0.1,
		max: 5,
		step: 0.1,
		displayStepper: true,
	},
	logoColor: {
		type: ControlType.Color,
		title: "Logo Color",
		defaultValue: "#000000",
	},
	buttonFont: {
		type: ControlType.Font,
		title: "Button Font",
		defaultValue: {
			fontSize: "14px",
			variant: "Medium",
			letterSpacing: "-0.01em",
			lineHeight: "1em",
		},
		controls: "extended",
		defaultFontType: "sans-serif",
	},
	buttonColor: {
		type: ControlType.Color,
		title: "Button Color",
		defaultValue: "#000000",
	},
	buttonTextColor: {
		type: ControlType.Color,
		title: "Button Text Color",
		defaultValue: "#FFFFFF",
	},
	buttonPadding: {
		type: ControlType.Number,
		title: "Button Padding",
		defaultValue: 10,
		min: 4,
		max: 30,
		step: 1,
		unit: "px",
	},
	buttonOffsetX: {
		type: ControlType.Number,
		title: "Button Offset X",
		defaultValue: 20,
		min: -200,
		max: 200,
		step: 1,
		unit: "px",
	},
	buttonOffsetY: {
		type: ControlType.Number,
		title: "Button Offset Y",
		defaultValue: 0,
		min: -200,
		max: 200,
		step: 1,
		unit: "px",
	},
});

// virtual:walking-logo-1
import { WithFramerBreakpoints } from "unframer";
import { jsx } from "react/jsx-runtime";
var locales = [];
var defaultResponsiveVariants = {};
/** @type {function(Props): any} */
function ComponentWithRoot({ locale, ...rest }) {
	return (
		<ContextProviders
			routes={routes}
			framerSiteId={
				"fbc126bea31ebbe6cf64cc6db71380905aa865df749edb6fe0a120a118d79a8c"
			}
			locale={locale}
			locales={locales}
		>
			<WalkingLogo {...rest} />
		</ContextProviders>
	);
}
/**
 * @type {import("unframer").UnframerBreakpoint}
 * Represents a responsive breakpoint for unframer.
 */

/**
 * @typedef VariantsMap
 * Partial record of UnframerBreakpoint to Props.variant, with a mandatory 'base' key.
 * { [key in UnframerBreakpoint]?: Props['variant'] } & { base: Props['variant'] }
 */

/**
 * Renders WalkingLogo1FramerComponent for all breakpoints with a variants map. Variant prop is inferred per breakpoint.
 * @function
 * @param {Omit<Props, 'variant'> & {variants?: VariantsMap}} props
 * @returns {any}
 */
ComponentWithRoot.Responsive = ({ locale = "", ...rest }) => {
	return (
		<ContextProviders
			routes={routes}
			framerSiteId={
				"fbc126bea31ebbe6cf64cc6db71380905aa865df749edb6fe0a120a118d79a8c"
			}
			locale={locale}
			locales={locales}
		>
			<WithFramerBreakpoints
				Component={WalkingLogo}
				variants={defaultResponsiveVariants}
				{...rest}
			/>
		</ContextProviders>
	);
};
Object.assign(ComponentWithRoot, WalkingLogo);
var walking_logo_1_default = ComponentWithRoot;
export { walking_logo_1_default as default };
