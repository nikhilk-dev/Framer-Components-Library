// @ts-nocheck
/* eslint-disable */
/* This file was generated by Unframer for Framer project fbc126bea31ebbe6 "Untitled", do not edit manually */
"use client";

/**
 * @typedef Locale
 * string
 */

/**
 * @typedef {{
 * children?: React.ReactNode
 * locale?: Locale
 * style?: React.CSSProperties
 * className?: string
 * id?: string
 * ref?: any
 * width?: any
 * height?: any
 * layoutId?: string
 * direction?: 'Horizontal' | 'Vertical' // Direction
 * snapAlign?: 'Start' | 'Center' | 'End' // Snap Align
 * snapType?: 'Mandatory' | 'Proximity' // Snap Type
}} Props

 */
import { routes } from "./chunks/chunk-2F3PHDTH.js";

// virtual:snap-scroll
import { Fragment } from "react";
import { ContextProviders } from "unframer";

// /:https://framerusercontent.com/modules/iqZ9mZHGdaW444O1iVCu/TD0hNUqeUJDyfzsT14Ua/SnapScroll.js
import { jsx as _jsx } from "react/jsx-runtime";
import { useRef, useEffect } from "react";
import { addPropertyControls, ControlType } from "unframer";
var __unframerWindow = typeof window !== "undefined" ? window : void 0;
function SnapScroll(props) {
	const {
		direction = "vertical",
		snapAlign = "start",
		snapType = "mandatory",
		style,
	} = props;
	const ref = useRef(null);
	const isHorizontal = direction === "horizontal";
	useEffect(() => {
		if (!ref.current) return;
		let parent = ref.current.parentElement;
		let scrollContainer = null;
		while (parent) {
			const styles = __unframerWindow.getComputedStyle(parent);
			const overflowY = styles.overflowY;
			const overflowX = styles.overflowX;
			const isScrollable =
				overflowY === "scroll" ||
				overflowY === "auto" ||
				overflowX === "scroll" ||
				overflowX === "auto";
			const hasScrollableContent = isHorizontal
				? parent.scrollWidth > parent.clientWidth
				: parent.scrollHeight > parent.clientHeight;
			if (isScrollable || hasScrollableContent) {
				scrollContainer = parent;
				break;
			}
			if (parent === document.body) {
				break;
			}
			parent = parent.parentElement;
		}
		if (!scrollContainer) {
			console.warn(
				"SnapScroll: No scrollable parent found. Make sure this component is inside a Stack with scroll enabled.",
			);
			return;
		}
		const originalScrollSnapType = scrollContainer.style.scrollSnapType;
		const originalWebkitOverflowScrolling =
			scrollContainer.style.WebkitOverflowScrolling;
		const snapDirection = isHorizontal ? "x" : "y";
		scrollContainer.style.scrollSnapType = `${snapDirection} ${snapType}`;
		scrollContainer.style.WebkitOverflowScrolling = "touch";
		scrollContainer.offsetHeight;
		const children = Array.from(scrollContainer.children);
		const originalStyles = /* @__PURE__ */ new Map();
		children.forEach((child) => {
			if (child === ref.current || child.contains(ref.current)) {
				return;
			}
			originalStyles.set(child, {
				snapAlign: child.style.scrollSnapAlign,
				snapStop: child.style.scrollSnapStop,
			});
			child.style.scrollSnapAlign = snapAlign;
			child.style.scrollSnapStop = "always";
		});
		return () => {
			if (scrollContainer) {
				scrollContainer.style.scrollSnapType = originalScrollSnapType;
				scrollContainer.style.WebkitOverflowScrolling =
					originalWebkitOverflowScrolling;
			}
			children.forEach((child) => {
				const original = originalStyles.get(child);
				if (original) {
					child.style.scrollSnapAlign = original.snapAlign;
					child.style.scrollSnapStop = original.snapStop;
				}
			});
		};
	}, [direction, snapAlign, snapType, isHorizontal]);
	return (
		<div
			ref={ref}
			style={{
				...style,
				minWidth: "5px",
				minHeight: "5px",
				pointerEvents: "none",
				opacity: 0.01,
				position: "relative",
				flexShrink: 0,
			}}
		/>
	);
}
addPropertyControls(SnapScroll, {
	direction: {
		type: ControlType.Enum,
		title: "Direction",
		options: ["horizontal", "vertical"],
		optionTitles: ["Horizontal", "Vertical"],
		defaultValue: "vertical",
		displaySegmentedControl: true,
	},
	snapAlign: {
		type: ControlType.Enum,
		title: "Snap Align",
		options: ["start", "center", "end"],
		optionTitles: ["Start", "Center", "End"],
		defaultValue: "start",
		displaySegmentedControl: true,
	},
	snapType: {
		type: ControlType.Enum,
		title: "Snap Type",
		options: ["mandatory", "proximity"],
		optionTitles: ["Mandatory", "Proximity"],
		defaultValue: "mandatory",
		displaySegmentedControl: true,
	},
});

// virtual:snap-scroll
import { WithFramerBreakpoints } from "unframer";
import { jsx } from "react/jsx-runtime";
var locales = [];
var defaultResponsiveVariants = {};
/** @type {function(Props): any} */
function ComponentWithRoot({ locale, ...rest }) {
	return (
		<ContextProviders
			routes={routes}
			framerSiteId={
				"fbc126bea31ebbe6cf64cc6db71380905aa865df749edb6fe0a120a118d79a8c"
			}
			locale={locale}
			locales={locales}
		>
			<SnapScroll {...rest} />
		</ContextProviders>
	);
}
/**
 * @type {import("unframer").UnframerBreakpoint}
 * Represents a responsive breakpoint for unframer.
 */

/**
 * @typedef VariantsMap
 * Partial record of UnframerBreakpoint to Props.variant, with a mandatory 'base' key.
 * { [key in UnframerBreakpoint]?: Props['variant'] } & { base: Props['variant'] }
 */

/**
 * Renders SnapScrollFramerComponent for all breakpoints with a variants map. Variant prop is inferred per breakpoint.
 * @function
 * @param {Omit<Props, 'variant'> & {variants?: VariantsMap}} props
 * @returns {any}
 */
ComponentWithRoot.Responsive = ({ locale = "", ...rest }) => {
	return (
		<ContextProviders
			routes={routes}
			framerSiteId={
				"fbc126bea31ebbe6cf64cc6db71380905aa865df749edb6fe0a120a118d79a8c"
			}
			locale={locale}
			locales={locales}
		>
			<WithFramerBreakpoints
				Component={SnapScroll}
				variants={defaultResponsiveVariants}
				{...rest}
			/>
		</ContextProviders>
	);
};
Object.assign(ComponentWithRoot, SnapScroll);
var snap_scroll_default = ComponentWithRoot;
export { snap_scroll_default as default };
