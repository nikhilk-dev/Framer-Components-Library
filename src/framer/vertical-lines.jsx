// @ts-nocheck
/* eslint-disable */
/* This file was generated by Unframer for Framer project fbc126bea31ebbe6 "Untitled", do not edit manually */
"use client";

/**
 * @typedef Locale
 * string
 */

/**
 * @typedef {{
 * children?: React.ReactNode
 * locale?: Locale
 * style?: React.CSSProperties
 * className?: string
 * id?: string
 * ref?: any
 * width?: any
 * height?: any
 * layoutId?: string
 * lineCount?: number // Line Count
 * lineColor?: string // Line Color
 * lineWidth?: number // Line Width
 * groupSpacing?: number // Group Spacing
 * leftPadding?: number // Left Padding
 * rightPadding?: number // Right Padding
 * spacing?: 'Equal' | 'Custom' // Spacing
 * customSpacing?: number // Custom Spacing
 * opacity?: number // Opacity
}} Props

 */
import { routes } from "./chunks/chunk-2F3PHDTH.js";

// virtual:vertical-lines
import { Fragment } from "react";
import { ContextProviders } from "unframer";

// /:https://framerusercontent.com/modules/4l951QXgCE0VaLAqUXay/fHUOJNvw0SL2LM7TPAFq/VerticalLines.js
import { jsx as _jsx } from "react/jsx-runtime";
import { addPropertyControls, ControlType } from "unframer";
function VerticalLines(props) {
	const {
		lineCount = 6,
		lineColor = "#EEEEEE",
		lineWidth = 1,
		spacing = "equal",
		customSpacing = 100,
		opacity = 1,
		groupSpacing = 20,
		leftPadding = 0,
		rightPadding = 0,
		style,
	} = props;
	const groupCount = Math.ceil(lineCount / 2);
	const groups = Array.from(
		{
			length: groupCount,
		},
		(_, i) => {
			const linesInGroup = i === groupCount - 1 && lineCount % 2 === 1 ? 1 : 2;
			return linesInGroup;
		},
	);
	return (
		<div
			style={{
				...style,
				position: "fixed",
				top: 0,
				left: 0,
				width: "100%",
				height: "100%",
				display: "flex",
				justifyContent: spacing === "equal" ? "space-between" : "flex-start",
				alignItems: "stretch",
				pointerEvents: "none",
				zIndex: -1,
				paddingLeft: `${leftPadding}px`,
				paddingRight: `${rightPadding}px`,
			}}
		>
			{groups.map((linesInGroup, groupIndex) => (
				<div
					style={{
						display: "flex",
						gap: `${groupSpacing}px`,
						marginLeft:
							spacing === "custom" && groupIndex > 0
								? `${customSpacing}px`
								: void 0,
					}}
				>
					{Array.from(
						{
							length: linesInGroup,
						},
						(_, lineIndex) => (
							<div
								style={{
									width: `${lineWidth}px`,
									height: "100%",
									backgroundColor: lineColor,
									opacity,
								}}
							/>
						),
					)}
				</div>
			))}
		</div>
	);
}
addPropertyControls(VerticalLines, {
	lineCount: {
		type: ControlType.Number,
		title: "Line Count",
		defaultValue: 6,
		min: 1,
		max: 50,
		step: 1,
		displayStepper: true,
	},
	lineColor: {
		type: ControlType.Color,
		title: "Line Color",
		defaultValue: "#EEEEEE",
	},
	lineWidth: {
		type: ControlType.Number,
		title: "Line Width",
		defaultValue: 1,
		min: 1,
		max: 10,
		step: 1,
		unit: "px",
	},
	groupSpacing: {
		type: ControlType.Number,
		title: "Group Spacing",
		defaultValue: 20,
		min: 0,
		max: 100,
		step: 5,
		unit: "px",
	},
	leftPadding: {
		type: ControlType.Number,
		title: "Left Padding",
		defaultValue: 0,
		min: 0,
		max: 500,
		step: 10,
		unit: "px",
	},
	rightPadding: {
		type: ControlType.Number,
		title: "Right Padding",
		defaultValue: 0,
		min: 0,
		max: 500,
		step: 10,
		unit: "px",
	},
	spacing: {
		type: ControlType.Enum,
		title: "Spacing",
		options: ["equal", "custom"],
		optionTitles: ["Equal", "Custom"],
		defaultValue: "equal",
		displaySegmentedControl: true,
	},
	customSpacing: {
		type: ControlType.Number,
		title: "Custom Spacing",
		defaultValue: 100,
		min: 10,
		max: 500,
		step: 10,
		unit: "px",
		hidden: ({ spacing }) => spacing === "equal",
	},
	opacity: {
		type: ControlType.Number,
		title: "Opacity",
		defaultValue: 1,
		min: 0,
		max: 1,
		step: 0.1,
	},
});

// virtual:vertical-lines
import { WithFramerBreakpoints } from "unframer";
import { jsx } from "react/jsx-runtime";
var locales = [];
var defaultResponsiveVariants = {};
/** @type {function(Props): any} */
function ComponentWithRoot({ locale, ...rest }) {
	return (
		<ContextProviders
			routes={routes}
			framerSiteId={
				"fbc126bea31ebbe6cf64cc6db71380905aa865df749edb6fe0a120a118d79a8c"
			}
			locale={locale}
			locales={locales}
		>
			<VerticalLines {...rest} />
		</ContextProviders>
	);
}
/**
 * @type {import("unframer").UnframerBreakpoint}
 * Represents a responsive breakpoint for unframer.
 */

/**
 * @typedef VariantsMap
 * Partial record of UnframerBreakpoint to Props.variant, with a mandatory 'base' key.
 * { [key in UnframerBreakpoint]?: Props['variant'] } & { base: Props['variant'] }
 */

/**
 * Renders VerticalLinesFramerComponent for all breakpoints with a variants map. Variant prop is inferred per breakpoint.
 * @function
 * @param {Omit<Props, 'variant'> & {variants?: VariantsMap}} props
 * @returns {any}
 */
ComponentWithRoot.Responsive = ({ locale = "", ...rest }) => {
	return (
		<ContextProviders
			routes={routes}
			framerSiteId={
				"fbc126bea31ebbe6cf64cc6db71380905aa865df749edb6fe0a120a118d79a8c"
			}
			locale={locale}
			locales={locales}
		>
			<WithFramerBreakpoints
				Component={VerticalLines}
				variants={defaultResponsiveVariants}
				{...rest}
			/>
		</ContextProviders>
	);
};
Object.assign(ComponentWithRoot, VerticalLines);
var vertical_lines_default = ComponentWithRoot;
export { vertical_lines_default as default };
