// @ts-nocheck
/* eslint-disable */
/* This file was generated by Unframer for Framer project fbc126bea31ebbe6 "Untitled", do not edit manually */
"use client";

/**
 * @typedef Locale
 * string
 */

/**
 * @typedef {{
 * children?: React.ReactNode
 * locale?: Locale
 * style?: React.CSSProperties
 * className?: string
 * id?: string
 * ref?: any
 * width?: any
 * height?: any
 * layoutId?: string
 * navItems?: {label: string, targetId: string, pageLink: string[] // Nav Items
 * showBottomItems?: boolean // Show Bottom Items
 * bottomIcons?: {icon: string, link: string, customColor: string[] // Bottom Icons
 * alignment?: 'Top' | 'Center' | 'Bottom' // Alignment
 * horizontal?: 'Left' | 'Center' | 'Right' // Horizontal
 * bottomAlignment?: 'Top' | 'Center' | 'Bottom' // Bottom Alignment
 * bottomHorizontal?: 'Left' | 'Center' | 'Right' // Bottom Horizontal
 * background?: string // Background
 * textColor?: string // Text Color
 * activeColor?: string // Active Color
 * hoverColor?: string // Hover Color
 * highlightColor?: string // Highlight Color
 * highlightWidth?: number // Highlight Width
 * highlightHeight?: number // Highlight Height
 * highlightSpacing?: number // Highlight Spacing
 * highlightRadius?: number // Highlight Radius
 * iconColor?: string // Icon Color
 * font?: undefined // Font
 * activeFont?: undefined // Active Font
 * height?: number // Height
 * shadow?: boolean // Shadow
 * spacing?: number // Spacing
 * bottomSpacing?: number // Bottom Spacing
 * iconSize?: number // Icon Size
}} Props

 */
import { routes } from "./chunks/chunk-2F3PHDTH.js";

// virtual:sticky-navigation
import { Fragment } from "react";
import { ContextProviders } from "unframer";

// /:https://framerusercontent.com/modules/8qEfsNbapiLPLqDcMDtd/EjNc6eo4iPWYnIM12IuW/StickyNavigation.js
import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import { addPropertyControls, ControlType } from "unframer";
import { useState, useEffect, startTransition } from "react";
var __unframerWindow = typeof window !== "undefined" ? window : void 0;
function StickyNavigation(props) {
	const {
		items = [
			{
				label: "Home",
				targetId: "home",
			},
			{
				label: "About",
				targetId: "about",
			},
			{
				label: "Services",
				targetId: "services",
			},
			{
				label: "Contact",
				targetId: "contact",
			},
		],
		bottomItems = [
			{
				icon: '<svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg"><circle cx="12" cy="12" r="10" fill="currentColor"/></svg>',
				link: "https://example.com",
			},
			{
				icon: '<svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg"><rect x="4" y="4" width="16" height="16" fill="currentColor"/></svg>',
				link: "mailto:hello@example.com",
			},
		],
		backgroundColor = "#FFFFFF",
		textColor = "#000000",
		activeColor = "#0099FF",
		hoverColor = "#666666",
		iconColor = "#000000",
		highlightColor = "#0099FF",
		highlightWidth = 3,
		highlightHeight = 100,
		highlightSpacing = 12,
		highlightRadius = 0,
		font,
		activeFont,
		height = 60,
		shadow = true,
		spacing = 32,
		bottomSpacing = 16,
		iconSize = 24,
		alignment = "center",
		horizontalAlignment = "center",
		bottomAlignment = "flex-end",
		bottomHorizontalAlignment = "center",
		slideDirection = "left",
		showBottomItems = true,
		style,
	} = props;
	const [activeSection, setActiveSection] = useState("");
	const [hoveredItem, setHoveredItem] = useState(null);
	const [isVisible, setIsVisible] = useState(true);
	const [visibleItems, setVisibleItems] = useState(0);
	const [hasAnimated, setHasAnimated] = useState(false);
	const [visibleBottomItems, setVisibleBottomItems] = useState(0);
	useEffect(() => {
		if (typeof __unframerWindow === "undefined") return;
		const handleScroll = () => {
			const sections = items
				.map((item) => document.getElementById(item.targetId))
				.filter(Boolean);
			const archiveSection = document.getElementById("archive");
			let currentSection = "";
			const viewportHeight = __unframerWindow.innerHeight;
			const threshold = viewportHeight * 0.3;
			for (const section of sections) {
				if (section) {
					const rect = section.getBoundingClientRect();
					const visibleHeight =
						Math.min(rect.bottom, viewportHeight) - Math.max(rect.top, 0);
					if (rect.top <= threshold && rect.bottom > threshold) {
						currentSection = section.id;
						break;
					}
				}
			}
			if (currentSection && currentSection !== activeSection) {
				startTransition(() => setActiveSection(currentSection));
			}
			if (archiveSection) {
				const archiveRect = archiveSection.getBoundingClientRect();
				if (archiveRect.top <= viewportHeight * 0.5) {
					startTransition(() => setIsVisible(false));
				} else {
					startTransition(() => setIsVisible(true));
				}
			}
		};
		__unframerWindow.addEventListener("scroll", handleScroll);
		handleScroll();
		return () => __unframerWindow.removeEventListener("scroll", handleScroll);
	}, [items, activeSection]);
	useEffect(() => {
		if (typeof __unframerWindow === "undefined" || hasAnimated) return;
		const observer = new IntersectionObserver(
			(entries) => {
				entries.forEach((entry) => {
					if (entry.isIntersecting && !hasAnimated) {
						startTransition(() => setHasAnimated(true));
						items.forEach((_, index) => {
							setTimeout(() => {
								startTransition(() => setVisibleItems(index + 1));
							}, index * 150);
						});
						const mainItemsDelay = items.length * 150;
						bottomItems.forEach((_, index) => {
							setTimeout(
								() => {
									startTransition(() => setVisibleBottomItems(index + 1));
								},
								mainItemsDelay + index * 150,
							);
						});
					}
				});
			},
			{
				threshold: 0.1,
			},
		);
		const navElement = document.querySelector("[data-sticky-nav]");
		if (navElement) {
			observer.observe(navElement);
		}
		return () => observer.disconnect();
	}, [items, bottomItems, hasAnimated]);
	const handleClick = (targetId, pageLink) => {
		if (typeof __unframerWindow === "undefined") return;
		if (pageLink && pageLink.trim().length > 0) {
			if (
				pageLink.startsWith("http://") ||
				pageLink.startsWith("https://") ||
				pageLink.startsWith("mailto:")
			) {
				__unframerWindow.open(pageLink, "_blank", "noopener,noreferrer");
			} else {
				__unframerWindow.location.href = pageLink;
			}
			return;
		}
		const element = document.getElementById(targetId);
		if (element) {
			element.scrollIntoView({
				behavior: "smooth",
				block: "start",
			});
		}
	};
	return (
		<nav
			data-sticky-nav={true}
			style={{
				...style,
				position: "relative",
				backgroundColor,
				display: "flex",
				flexDirection: "column",
				justifyContent: "space-between",
				boxShadow: shadow ? "2px 0 8px rgba(0,0,0,0.08)" : "none",
				transform: isVisible
					? "translateX(0)"
					: slideDirection === "left"
						? "translateX(-100%)"
						: "translateX(100%)",
				transition: "transform 0.4s ease-in-out",
				...font,
			}}
		>
			<div
				style={{
					display: "flex",
					flexDirection: "column",
					alignItems: horizontalAlignment,
					justifyContent: alignment,
					gap: spacing,
				}}
			>
				{items.map((item, index) => {
					const isActive = item.targetId === activeSection;
					const isHovered = hoveredItem === item.targetId;
					const isItemVisible = index < visibleItems;
					return (
						<button
							onClick={() => handleClick(item.targetId, item.pageLink)}
							onMouseEnter={() =>
								startTransition(() => setHoveredItem(item.targetId))
							}
							onMouseLeave={() => startTransition(() => setHoveredItem(null))}
							style={{
								background: "none",
								border: "none",
								cursor: "pointer",
								padding: "4px 0px",
								paddingLeft: `${highlightSpacing}px`,
								color: isActive
									? activeColor
									: isHovered
										? hoverColor
										: textColor,
								transition:
									"color 0.2s ease, opacity 0.4s ease, transform 0.4s ease",
								opacity: isItemVisible ? 1 : 0,
								transform: isItemVisible
									? "translateX(0)"
									: "translateX(-20px)",
								position: "relative",
								width: "100%",
								textAlign:
									horizontalAlignment === "flex-start"
										? "left"
										: horizontalAlignment === "flex-end"
											? "right"
											: "center",
								...(isActive ? activeFont : font),
							}}
						>
							{isActive && (
								<div
									style={{
										position: "absolute",
										left: 0,
										top: "50%",
										transform: "translateY(-50%)",
										width: `${highlightWidth}px`,
										height: `${highlightHeight}%`,
										backgroundColor: highlightColor,
										borderRadius: `${highlightRadius}px`,
										transition: "opacity 0.2s ease",
									}}
								/>
							)}
							<span
								style={{
									transition: "border-color 0.2s ease",
								}}
							>
								{item.label}
								{isActive && "_"}
							</span>
						</button>
					);
				})}
			</div>
			{showBottomItems && (
				<div
					style={{
						display: "flex",
						flexDirection: "column",
						alignItems: bottomHorizontalAlignment,
						justifyContent: bottomAlignment,
						gap: bottomSpacing,
					}}
				>
					{bottomItems.map((item, index) => {
						const isHovered = hoveredItem === `bottom-${index}`;
						const isItemVisible = index < visibleBottomItems;
						const icon =
							item.icon ||
							'<svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg"><circle cx="12" cy="12" r="10" fill="currentColor"/></svg>';
						let processedIcon = icon
							.replace(/fill="#[0-9a-fA-F]{3,6}"/g, 'fill="currentColor"')
							.replace(/stroke="#[0-9a-fA-F]{3,6}"/g, 'stroke="currentColor"')
							.replace(/fill="rgba?\([^)]+\)"/g, 'fill="currentColor"')
							.replace(/stroke="rgba?\([^)]+\)"/g, 'stroke="currentColor"')
							.replace(
								/fill="(black|white|red|blue|green|yellow|orange|purple|pink|gray|grey)"/gi,
								'fill="currentColor"',
							)
							.replace(
								/stroke="(black|white|red|blue|green|yellow|orange|purple|pink|gray|grey)"/gi,
								'stroke="currentColor"',
							);
						processedIcon =
							item.customColor &&
							item.customColor.trim() !== "" &&
							item.customColor !== iconColor
								? processedIcon.replace(/currentColor/g, item.customColor)
								: processedIcon.replace(/currentColor/g, iconColor);
						return (
							<a
								href={item.link}
								target={"_blank"}
								rel={"noopener noreferrer"}
								onMouseEnter={() =>
									startTransition(() => setHoveredItem(`bottom-${index}`))
								}
								onMouseLeave={() => startTransition(() => setHoveredItem(null))}
								style={{
									display: "block",
									cursor: "pointer",
									padding: "4px 0px",
									transition: "opacity 0.4s ease, transform 0.4s ease",
									textDecoration: "none",
									opacity: isItemVisible ? (isHovered ? 0.7 : 1) : 0,
									transform: isItemVisible
										? "translateX(0)"
										: "translateX(-20px)",
								}}
							>
								<div
									dangerouslySetInnerHTML={{
										__html: processedIcon,
									}}
									style={{
										width: `${iconSize}px`,
										height: `${iconSize}px`,
										display: "flex",
										alignItems: "center",
										justifyContent: "center",
									}}
								/>
							</a>
						);
					})}
				</div>
			)}
		</nav>
	);
}
addPropertyControls(StickyNavigation, {
	items: {
		type: ControlType.Array,
		title: "Nav Items",
		control: {
			type: ControlType.Object,
			controls: {
				label: {
					type: ControlType.String,
					title: "Label",
					defaultValue: "Item",
				},
				targetId: {
					type: ControlType.String,
					title: "Target ID",
					defaultValue: "section",
				},
				pageLink: {
					type: ControlType.String,
					title: "Page Link (Optional)",
					defaultValue: "",
					placeholder: "https://... or /page",
				},
			},
		},
		defaultValue: [
			{
				label: "Home",
				targetId: "home",
			},
			{
				label: "About",
				targetId: "about",
			},
			{
				label: "Services",
				targetId: "services",
			},
			{
				label: "Contact",
				targetId: "contact",
			},
		],
	},
	showBottomItems: {
		type: ControlType.Boolean,
		title: "Show Bottom Items",
		defaultValue: true,
		enabledTitle: "Show",
		disabledTitle: "Hide",
	},
	bottomItems: {
		type: ControlType.Array,
		title: "Bottom Icons",
		control: {
			type: ControlType.Object,
			controls: {
				icon: {
					type: ControlType.String,
					title: "Icon SVG",
					displayTextArea: true,
					defaultValue:
						'<svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg"><circle cx="12" cy="12" r="10" fill="currentColor"/></svg>',
					placeholder:
						'Paste SVG code here...\nUse "currentColor" for theme-aware colors',
				},
				link: {
					type: ControlType.String,
					title: "Link",
					defaultValue: "https://example.com",
					placeholder: "https://...",
				},
				customColor: {
					type: ControlType.Color,
					title: "Custom Color",
				},
			},
		},
		defaultValue: [
			{
				icon: '<svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg"><circle cx="12" cy="12" r="10" fill="currentColor"/></svg>',
				link: "https://example.com",
			},
			{
				icon: '<svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg"><rect x="4" y="4" width="16" height="16" fill="currentColor"/></svg>',
				link: "mailto:hello@example.com",
			},
		],
		hidden: ({ showBottomItems }) => !showBottomItems,
	},
	alignment: {
		type: ControlType.Enum,
		title: "Alignment",
		options: ["flex-start", "center", "flex-end"],
		optionTitles: ["Top", "Center", "Bottom"],
		defaultValue: "center",
		displaySegmentedControl: true,
	},
	horizontalAlignment: {
		type: ControlType.Enum,
		title: "Horizontal",
		options: ["flex-start", "center", "flex-end"],
		optionTitles: ["Left", "Center", "Right"],
		defaultValue: "center",
		displaySegmentedControl: true,
	},
	bottomAlignment: {
		type: ControlType.Enum,
		title: "Bottom Alignment",
		options: ["flex-start", "center", "flex-end"],
		optionTitles: ["Top", "Center", "Bottom"],
		defaultValue: "flex-end",
		displaySegmentedControl: true,
		hidden: ({ showBottomItems }) => !showBottomItems,
	},
	bottomHorizontalAlignment: {
		type: ControlType.Enum,
		title: "Bottom Horizontal",
		options: ["flex-start", "center", "flex-end"],
		optionTitles: ["Left", "Center", "Right"],
		defaultValue: "center",
		displaySegmentedControl: true,
		hidden: ({ showBottomItems }) => !showBottomItems,
	},
	backgroundColor: {
		type: ControlType.Color,
		title: "Background",
		defaultValue: "#FFFFFF",
	},
	textColor: {
		type: ControlType.Color,
		title: "Text Color",
		defaultValue: "#000000",
	},
	activeColor: {
		type: ControlType.Color,
		title: "Active Color",
		defaultValue: "#0099FF",
	},
	hoverColor: {
		type: ControlType.Color,
		title: "Hover Color",
		defaultValue: "#666666",
	},
	highlightColor: {
		type: ControlType.Color,
		title: "Highlight Color",
		defaultValue: "#0099FF",
	},
	highlightWidth: {
		type: ControlType.Number,
		title: "Highlight Width",
		defaultValue: 3,
		min: 1,
		max: 10,
		step: 1,
		unit: "px",
	},
	highlightHeight: {
		type: ControlType.Number,
		title: "Highlight Height",
		defaultValue: 100,
		min: 10,
		max: 100,
		step: 5,
		unit: "%",
	},
	highlightSpacing: {
		type: ControlType.Number,
		title: "Highlight Spacing",
		defaultValue: 12,
		min: 0,
		max: 50,
		step: 1,
		unit: "px",
	},
	highlightRadius: {
		type: ControlType.Number,
		title: "Highlight Radius",
		defaultValue: 0,
		min: 0,
		max: 20,
		step: 1,
		unit: "px",
	},
	iconColor: {
		type: ControlType.Color,
		title: "Icon Color",
		defaultValue: "#000000",
	},
	font: {
		type: ControlType.Font,
		title: "Font",
		controls: "extended",
		defaultFontType: "sans-serif",
		defaultValue: {
			fontSize: "14px",
			variant: "Semibold",
			letterSpacing: "-0.01em",
			lineHeight: "1em",
		},
	},
	activeFont: {
		type: ControlType.Font,
		title: "Active Font",
		controls: "extended",
		defaultFontType: "sans-serif",
		defaultValue: {
			fontSize: "14px",
			variant: "Semibold",
			letterSpacing: "-0.01em",
			lineHeight: "1em",
		},
	},
	height: {
		type: ControlType.Number,
		title: "Height",
		defaultValue: 60,
		min: 40,
		max: 100,
		step: 1,
		unit: "px",
	},
	shadow: {
		type: ControlType.Boolean,
		title: "Shadow",
		defaultValue: true,
		enabledTitle: "Show",
		disabledTitle: "Hide",
	},
	spacing: {
		type: ControlType.Number,
		title: "Spacing",
		defaultValue: 4,
		min: 0,
		max: 100,
		step: 1,
		unit: "px",
	},
	bottomSpacing: {
		type: ControlType.Number,
		title: "Bottom Spacing",
		defaultValue: 16,
		min: 0,
		max: 100,
		step: 1,
		unit: "px",
		hidden: ({ showBottomItems }) => !showBottomItems,
	},
	iconSize: {
		type: ControlType.Number,
		title: "Icon Size",
		defaultValue: 24,
		min: 12,
		max: 64,
		step: 1,
		unit: "px",
		hidden: ({ showBottomItems }) => !showBottomItems,
	},
});

// virtual:sticky-navigation
import { WithFramerBreakpoints } from "unframer";
import { jsx } from "react/jsx-runtime";
var locales = [];
var defaultResponsiveVariants = {};
/** @type {function(Props): any} */
function ComponentWithRoot({ locale, ...rest }) {
	return (
		<ContextProviders
			routes={routes}
			framerSiteId={
				"fbc126bea31ebbe6cf64cc6db71380905aa865df749edb6fe0a120a118d79a8c"
			}
			locale={locale}
			locales={locales}
		>
			<StickyNavigation {...rest} />
		</ContextProviders>
	);
}
/**
 * @type {import("unframer").UnframerBreakpoint}
 * Represents a responsive breakpoint for unframer.
 */

/**
 * @typedef VariantsMap
 * Partial record of UnframerBreakpoint to Props.variant, with a mandatory 'base' key.
 * { [key in UnframerBreakpoint]?: Props['variant'] } & { base: Props['variant'] }
 */

/**
 * Renders StickyNavigationFramerComponent for all breakpoints with a variants map. Variant prop is inferred per breakpoint.
 * @function
 * @param {Omit<Props, 'variant'> & {variants?: VariantsMap}} props
 * @returns {any}
 */
ComponentWithRoot.Responsive = ({ locale = "", ...rest }) => {
	return (
		<ContextProviders
			routes={routes}
			framerSiteId={
				"fbc126bea31ebbe6cf64cc6db71380905aa865df749edb6fe0a120a118d79a8c"
			}
			locale={locale}
			locales={locales}
		>
			<WithFramerBreakpoints
				Component={StickyNavigation}
				variants={defaultResponsiveVariants}
				{...rest}
			/>
		</ContextProviders>
	);
};
Object.assign(ComponentWithRoot, StickyNavigation);
var sticky_navigation_default = ComponentWithRoot;
export { sticky_navigation_default as default };
