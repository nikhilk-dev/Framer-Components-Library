// @ts-nocheck
/* eslint-disable */
/* This file was generated by Unframer for Framer project fbc126bea31ebbe6 "Untitled", do not edit manually */
"use client";

/**
 * @typedef Locale
 * string
 */

/**
 * @typedef {{
 * children?: React.ReactNode
 * locale?: Locale
 * style?: React.CSSProperties
 * className?: string
 * id?: string
 * ref?: any
 * width?: any
 * height?: any
 * layoutId?: string
 * buttonText?: string // Button Text
 * background?: string // Background
 * hoverBackground?: string // Hover Background
 * textColor?: string // Text Color
 * hoverTextColor?: string // Hover Text Color
 * buttonFont?: undefined // Button Font
 * padding?: undefined // Padding
 * borderRadius?: number // Border Radius
 * shadow?: boolean // Shadow
}} Props

 */
import { routes } from "./chunks/chunk-2F3PHDTH.js";

// virtual:scroll-to-top
import { Fragment } from "react";
import { ContextProviders } from "unframer";

// /:https://framerusercontent.com/modules/0l9771qwgZlb5vUuiuTC/ejS4wT7ke3K7YB3W7jfZ/ScrollToTop.js
import { jsx as _jsx } from "react/jsx-runtime";
import { addPropertyControls, ControlType, RenderTarget } from "unframer";
import { useEffect, useState, startTransition } from "react";
var __unframerWindow = typeof window !== "undefined" ? window : void 0;
function ScrollToTop(props) {
	const {
		buttonText,
		backgroundColor,
		textColor,
		buttonFont,
		borderRadius,
		shadow,
		padding,
		hoverBackgroundColor,
		hoverTextColor,
	} = props;
	const [isVisible, setIsVisible] = useState(false);
	const [isHovered, setIsHovered] = useState(false);
	useEffect(() => {
		if (typeof __unframerWindow === "undefined") return;
		const toggleVisibility = () => {
			if (__unframerWindow.scrollY > 300) {
				startTransition(() => setIsVisible(true));
			} else {
				startTransition(() => setIsVisible(false));
			}
		};
		__unframerWindow.addEventListener("scroll", toggleVisibility);
		toggleVisibility();
		return () =>
			__unframerWindow.removeEventListener("scroll", toggleVisibility);
	}, []);
	const scrollToTop = () => {
		if (typeof __unframerWindow === "undefined") return;
		try {
			__unframerWindow.scrollTo({
				top: 0,
				behavior: "smooth",
			});
		} catch (error) {
			__unframerWindow.scrollTo(0, 0);
		}
	};
	const isCanvas = RenderTarget.current() === RenderTarget.canvas;
	if (!isVisible && !isCanvas) return null;
	return (
		<button
			onClick={scrollToTop}
			style={{
				position: "relative",
				width: "max-content",
				backgroundColor: isHovered ? hoverBackgroundColor : backgroundColor,
				color: isHovered ? hoverTextColor : textColor,
				border: "none",
				borderRadius,
				padding,
				cursor: "pointer",
				boxShadow: shadow ? "0 4px 10px rgba(0,0,0,0.15)" : "none",
				transition: "all 0.3s ease",
				opacity: !isVisible && isCanvas ? 0.5 : 1,
				whiteSpace: "nowrap",
				...buttonFont,
			}}
			onMouseEnter={(e) => {
				setIsHovered(true);
				e.currentTarget.style.transform = "translateY(-2px)";
				e.currentTarget.style.boxShadow = shadow
					? "0 6px 15px rgba(0,0,0,0.2)"
					: "none";
			}}
			onMouseLeave={(e) => {
				setIsHovered(false);
				e.currentTarget.style.transform = "translateY(0)";
				e.currentTarget.style.boxShadow = shadow
					? "0 4px 10px rgba(0,0,0,0.15)"
					: "none";
			}}
			aria-label={"Scroll to top"}
		>
			{buttonText}
		</button>
	);
}
addPropertyControls(ScrollToTop, {
	buttonText: {
		type: ControlType.String,
		title: "Button Text",
		defaultValue: "\u2191 Top",
	},
	backgroundColor: {
		type: ControlType.Color,
		title: "Background",
		defaultValue: "#000000",
	},
	hoverBackgroundColor: {
		type: ControlType.Color,
		title: "Hover Background",
		defaultValue: "#333333",
	},
	textColor: {
		type: ControlType.Color,
		title: "Text Color",
		defaultValue: "#FFFFFF",
	},
	hoverTextColor: {
		type: ControlType.Color,
		title: "Hover Text Color",
		defaultValue: "#FFFFFF",
	},
	buttonFont: {
		type: ControlType.Font,
		title: "Button Font",
		defaultValue: {
			variant: "Semibold",
			fontSize: "14px",
			letterSpacing: "-0.01em",
			lineHeight: "1em",
		},
		controls: "extended",
		defaultFontType: "sans-serif",
	},
	padding: {
		type: ControlType.Padding,
		title: "Padding",
		defaultValue: "12px 24px",
	},
	borderRadius: {
		type: ControlType.Number,
		title: "Border Radius",
		defaultValue: 8,
		min: 0,
		max: 50,
		step: 1,
		unit: "px",
	},
	shadow: {
		type: ControlType.Boolean,
		title: "Shadow",
		defaultValue: true,
		enabledTitle: "On",
		disabledTitle: "Off",
	},
});

// virtual:scroll-to-top
import { WithFramerBreakpoints } from "unframer";
import { jsx } from "react/jsx-runtime";
var locales = [];
var defaultResponsiveVariants = {};
/** @type {function(Props): any} */
function ComponentWithRoot({ locale, ...rest }) {
	return (
		<ContextProviders
			routes={routes}
			framerSiteId={
				"fbc126bea31ebbe6cf64cc6db71380905aa865df749edb6fe0a120a118d79a8c"
			}
			locale={locale}
			locales={locales}
		>
			<ScrollToTop {...rest} />
		</ContextProviders>
	);
}
/**
 * @type {import("unframer").UnframerBreakpoint}
 * Represents a responsive breakpoint for unframer.
 */

/**
 * @typedef VariantsMap
 * Partial record of UnframerBreakpoint to Props.variant, with a mandatory 'base' key.
 * { [key in UnframerBreakpoint]?: Props['variant'] } & { base: Props['variant'] }
 */

/**
 * Renders ScrollToTopFramerComponent for all breakpoints with a variants map. Variant prop is inferred per breakpoint.
 * @function
 * @param {Omit<Props, 'variant'> & {variants?: VariantsMap}} props
 * @returns {any}
 */
ComponentWithRoot.Responsive = ({ locale = "", ...rest }) => {
	return (
		<ContextProviders
			routes={routes}
			framerSiteId={
				"fbc126bea31ebbe6cf64cc6db71380905aa865df749edb6fe0a120a118d79a8c"
			}
			locale={locale}
			locales={locales}
		>
			<WithFramerBreakpoints
				Component={ScrollToTop}
				variants={defaultResponsiveVariants}
				{...rest}
			/>
		</ContextProviders>
	);
};
Object.assign(ComponentWithRoot, ScrollToTop);
var scroll_to_top_default = ComponentWithRoot;
export { scroll_to_top_default as default };
